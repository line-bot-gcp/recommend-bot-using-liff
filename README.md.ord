# flask-sample
flaskにて実装したサンプルです。
python3を前提とします。

必要なパッケージは下記コマンドでインストール

```
pip3 install -r requirements.txt
```
確認するにはこちら
```
pip3 freeze
```

完了したら実行する。

```cmd
$ python -V
Python 3.*.*
```

下記コマンドにて実行する

```
python app.py
```
立ちあがっったサーバにアクセスして画面が映るとOK


